<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/Widget.Styled.ActionBar</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="colorPrimary">@color/md_teal_500</item>
        <item name="colorPrimaryDark">@color/md_teal_700</item>
        <item name="colorAccent">@color/md_deep_orange_A700</item>
        <item name="android:textColorPrimary">@color/primary_text_default_material_dark</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Widget.Styled.ActionBar" parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:displayOptions">@null</item>
    </style>

</resources>
